#include <iostream>
#include <cstdio>
using namespace std;
const int MOD = 10007, MAXN = 100000 + 2;

int num[MAXN], color[MAXN];

int main()
{
    int n ,m;
    cin >> n >> m;
    for (int i = 1; i <= n; ++i)
    {
        int a;
        scanf("%d", &a);
        num[i] = a
    }
    for (int i = 1; i <= n; ++i)
    {
        int a;
        scanf("%d", &a);
        color[i] = a
    }

    int sumn = (n+1)/2*n % MOD;
    for (int i = 1; i <= n; ++i)
    {

    }
}
